# For neovim config
1. Install Rust
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

2. Rust-anlyzer is requried for the rust LSP in neovim.
```bash
rustup component add rust-analyzer
```
